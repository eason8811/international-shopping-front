import {AccountAddressWorkspace} from "@/features/account/components/account-address-workspace";

/**
 * 账号与地址页
 *
 * @returns 页面节点
 */
export default function AccountPage() {
    return <AccountAddressWorkspace initialTab="account"/>;
}
