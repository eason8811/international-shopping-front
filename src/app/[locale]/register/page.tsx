/**
 * 注册页, 提供账号创建与邮箱验证流程
 *
 * @returns 注册页面
 */
export default function RegisterPage() {
    return (
        <main className="flex min-h-screen items-center justify-center bg-background px-6 py-16">
            <section className="w-full max-w-2xl rounded-3xl border border-border bg-card p-8 shadow-sm">
                <p className="text-sm font-medium uppercase tracking-[0.18em] text-muted-foreground">
                    Route placeholder
                </p>
                <h1 className="mt-4 text-3xl font-semibold text-foreground">Register UI removed</h1>
                <p className="mt-4 text-sm leading-6 text-muted-foreground">
                    Registration request logic stays in the auth feature and BFF routes. The visual flow will be
                    rebuilt from scratch with the updated design system.
                </p>
            </section>
        </main>
    );
}
